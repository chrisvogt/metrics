{
  "name": "metrics",
  "private": true,
  "description": "Personal metrics – Firebase Hosting + Cloud Functions",
  "scripts": {
    "build": "npm run build:hosting",
    "build:hosting": "cd hosting && npm ci && npm run build",
    "deploy:all": "pnpm run build && firebase deploy",
    "deploy:hosting": "npm run build:hosting && firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions"
  },
  "engines": {
    "node": ">=24"
  }
}
